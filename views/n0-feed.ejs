<!-- <%- include('partials/header') -%> -->
  <div class="container">
    <div class="row justify-content-center mt-5">
      <ul class="row list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <!-- <a href="/post/<%= posts[i]._id%>"></a> -->
              <img class="img-fluid" src="<%= posts[i].image%>">
            <!-- </a> -->
            <div class="row justify-content-between">
              <form  class="col-10" 
              action="/post/commentPost/<%= posts2[i]._id %>?_method=PUT"  
              enctype="multipart/form-data"
              method="POST"
              >
              <div class="mb-3">
                <label for="caption" class="form-label">Comment</label>
                <input type="text" class="form-control" id="comments"  name="comments"></input>
              </div>
            <button type="submit" class="btn btn-primary" value="submit" >Submit this one!</button>
                        </form>
    
          </li>
        <% } %>
        </ul>
    </div>
  </div>  
<%- include('partials/footer') -%>